{
  "name": "learn-claude-code",
  "version": "0.0.1",
  "main": "dist/agent.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node dist/agent.js",
    "start:anthropic": "npm run build && MODEL_PROVIDER=anthropic node dist/agent.js",
    "start:openai": "npm run build && MODEL_PROVIDER=openai node dist/agent.js",
    "start:s01": "npm run build && node dist/s01_agent_loop.js",
    "start:s02": "npm run build && node dist/s02_openai_agent_loop.js",
    "dev": "tsc --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@anthropic-ai/sdk": "^0.79.0",
    "openai": "^4.52.0",
    "dotenv": "^17.3.1"
  },
  "devDependencies": {
    "typescript": "^5.5.3",
    "@types/node": "^20.11.0"
  }
}
